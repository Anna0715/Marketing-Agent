# 系统架构设计文档

## 1. 总体架构

本系统采用分层架构设计，主要包括以下几个层次：

```
┌─────────────────────────────────────────────────────┐
│              展示层 (Presentation Layer)              │
│         FastAPI RESTful API / Web界面                │
└─────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────┐
│             业务逻辑层 (Business Layer)               │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────┐ │
│  │ Agent核心    │  │ 知识库管理   │  │ 数据API   │ │
│  └──────────────┘  └──────────────┘  └───────────┘ │
└─────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────┐
│              数据访问层 (Data Access Layer)           │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────┐ │
│  │ 向量存储     │  │ 文档加载器   │  │ 外部API   │ │
│  │ (ChromaDB)   │  │              │  │ 客户端    │ │
│  └──────────────┘  └──────────────┘  └───────────┘ │
└─────────────────────────────────────────────────────┘
```

## 2. 核心组件设计

### 2.1 RAG知识库系统

#### 2.1.1 文档处理流程

```
文档输入 → 格式检测 → 文档加载 → 文本提取 → 分块处理 → 向量化 → 存储
```

**关键组件：**
- `DocumentLoaderFactory`: 文档加载器工厂，支持多种格式
- `BaseDocumentLoader`: 文档加载器基类，定义统一接口
- `TextSplitter`: 文本分块器，将长文本分割为合适的块

**支持的文档格式：**
- PDF: 使用PyPDF2提取文本
- PPTX: 使用python-pptx提取幻灯片文本
- DOCX: 使用python-docx提取段落文本
- TXT/MD: 直接读取文本内容
- 飞书文档: 通过飞书开放API获取内容

#### 2.1.2 向量存储架构

**技术选型：**
- 向量数据库: ChromaDB（支持持久化、相似度搜索）
- Embedding模型: OpenAI text-embedding-ada-002（可替换）

**存储结构：**
```python
{
    "id": "doc_source_chunk_idx",
    "embedding": [0.1, 0.2, ...],  # 向量表示
    "document": "文档内容",
    "metadata": {
        "source": "文件路径",
        "doc_type": "pdf/pptx/docx",
        "chunk_index": 0,
        "tags": ["标签1", "标签2"]
    }
}
```

**检索策略：**
1. 语义检索：基于向量相似度（余弦相似度）
2. 元数据过滤：支持按文档类型、标签等过滤
3. 混合检索：结合关键词和语义相似度（可扩展）

### 2.2 Agent系统

#### 2.2.1 Agent架构

采用LangChain的Agent框架，基于ReAct模式（Reasoning + Acting）：

```
用户请求 → Agent规划 → 工具调用 → 结果整合 → 生成响应
```

**Agent类型：** OpenAI Functions Agent
- 优势：结构化工具调用，更好的可控性
- 支持：函数式工具、流式响应、记忆管理

#### 2.2.2 工具系统

**可用工具：**

1. **KnowledgeBaseSearchTool**
   - 功能：在知识库中搜索相关信息
   - 输入：查询文本
   - 输出：相关文档列表（带相似度分数）

2. **CaseRetrievalTool**
   - 功能：检索客户案例
   - 输入：客户名称、行业、需求
   - 输出：匹配的客户案例

3. **CompanyDataQueryTool**
   - 功能：查询公司内部数据
   - 输入：公司名称、数据类型
   - 输出：公司数据（财务、销售、营销等）

4. **IndustryDataQueryTool**
   - 功能：查询行业数据
   - 输入：行业名称、指标
   - 输出：行业趋势数据

#### 2.2.3 解决方案生成流程

```
1. 解析用户请求（客户信息、需求、预算等）
   ↓
2. 从知识库检索相关案例和方案（RAG）
   ↓
3. 查询公司数据（如需要）
   ↓
4. 查询行业数据（如需要）
   ↓
5. Agent整合所有信息
   ↓
6. 生成结构化解决方案
   ↓
7. 返回结果（包含引用来源）
```

### 2.3 第三方数据API集成

#### 2.3.1 API客户端设计

**统一接口设计：**
```python
class ExternalDataAPI:
    - query_company_data()      # 公司数据
    - query_industry_data()     # 行业数据
    - query_customer_history()  # 客户历史
    - generic_query()           # 通用查询
```

**错误处理：**
- 网络超时重试（使用tenacity）
- API错误码映射
- 数据验证和清洗

**缓存策略：**
- 可选的Redis缓存（未来扩展）
- 本地缓存常用查询结果

## 3. 数据流设计

### 3.1 知识库构建流程

```
文档上传/添加
    ↓
格式检测和验证
    ↓
文档加载和解析
    ↓
文本提取和清洗
    ↓
分块处理（chunk_size=1000, overlap=200）
    ↓
向量化（Embedding）
    ↓
存储到ChromaDB
    ↓
返回文档元数据
```

### 3.2 解决方案生成流程

```
用户提交请求
    ↓
参数验证和标准化
    ↓
Agent接收任务
    ↓
并行执行：
  ├─ 知识库检索（案例、方案）
  ├─ 公司数据查询（如需要）
  └─ 行业数据查询（如需要）
    ↓
Agent整合信息
    ↓
生成解决方案（LLM）
    ↓
提取引用来源
    ↓
构建响应对象
    ↓
返回结果
```

## 4. 技术栈

### 4.1 核心框架

- **Web框架**: FastAPI（高性能、自动API文档）
- **LLM框架**: LangChain（Agent、工具、记忆管理）
- **向量数据库**: ChromaDB（轻量级、易部署）

### 4.2 文档处理

- **PDF**: PyPDF2
- **PPTX**: python-pptx
- **DOCX**: python-docx
- **文本**: 标准库

### 4.3 LLM和Embedding

- **LLM**: OpenAI GPT-4（可替换为其他模型）
- **Embedding**: OpenAI text-embedding-ada-002

### 4.4 数据存储

- **向量存储**: ChromaDB（本地持久化）
- **配置**: 环境变量 + YAML文件
- **缓存**: 内存（可扩展Redis）

## 5. 扩展性设计

### 5.1 支持新的文档格式

1. 继承 `BaseDocumentLoader`
2. 实现 `load()` 方法
3. 在 `DocumentLoaderFactory` 中注册

### 5.2 集成新的LLM

1. 修改 `src/utils/config.py` 添加配置
2. 在 `src/agent/agent.py` 中替换LLM初始化
3. 使用LangChain的统一接口

### 5.3 添加新的Agent工具

1. 继承 `BaseTool`
2. 实现 `_run()` 和 `_arun()` 方法
3. 在 `MarketingSolutionAgent._create_tools()` 中添加

### 5.4 替换向量数据库

- 支持FAISS（本地）
- 支持Pinecone（云端）
- 支持Weaviate（自托管）

## 6. 性能优化

### 6.1 检索优化

- 向量索引优化（HNSW算法）
- 批量检索
- 结果缓存

### 6.2 生成优化

- 流式生成（Streaming）
- 异步处理
- 并发请求处理

### 6.3 存储优化

- 文档去重
- 增量更新
- 压缩存储

## 7. 安全设计

### 7.1 API安全

- API密钥认证
- 请求频率限制
- 输入验证和清理

### 7.2 数据安全

- 敏感信息脱敏
- 访问权限控制
- 审计日志

### 7.3 配置安全

- 环境变量管理
- 密钥加密存储
- 配置文件权限

## 8. 监控和运维

### 8.1 日志系统

- 结构化日志（JSON格式）
- 日志级别管理
- 日志轮转

### 8.2 监控指标

- API响应时间
- 检索准确率
- 生成质量评分
- 系统资源使用

### 8.3 错误处理

- 统一异常处理
- 错误码定义
- 错误恢复机制

## 9. 部署架构

### 9.1 开发环境

```
单机部署
- 本地向量数据库
- 本地文件存储
- 开发服务器
```

### 9.2 生产环境

```
┌─────────────┐
│  负载均衡    │
└──────┬──────┘
       │
  ┌────┴────┐
  │         │
┌─▼───┐  ┌─▼───┐
│App 1│  │App 2│  (多实例)
└─┬───┘  └─┬───┘
  │        │
  └────┬───┘
       │
┌──────▼──────┐
│  向量数据库  │  (共享存储)
│  (ChromaDB) │
└─────────────┘
```

## 10. 未来优化方向

1. **多模态支持**: 处理图片、视频等多媒体内容
2. **实时更新**: 支持知识库的增量更新
3. **个性化推荐**: 基于用户历史优化推荐
4. **A/B测试**: 对比不同生成策略的效果
5. **可视化界面**: Web界面管理知识库和查看方案
